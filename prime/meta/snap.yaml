name: pheri
version: 0.1.0
summary: Terminal-based MySQL client
description: 'Pheri is a TUI for MySQL to interact with databases via terminal.

  '
apps:
  pheri:
    command: bin/pheri
    command-chain:
    - snap/command-chain/snapcraft-runner
architectures:
- amd64
assumes:
- command-chain
base: core20
confinement: strict
grade: stable
